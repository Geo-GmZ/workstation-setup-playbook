- hosts: localhost
  connection: local
  become: true

  pre_tasks: 
    - name: Update Repositories.
      apt: 
        update_cache: yes
      changed_when: False

  tasks:
    - include: tasks/cron.yml
    - include: tasks/packages.yml
